const form = document.getElementById('form');
const dorme = document.getElementById('horariodedormir');
const acorda = document.getElementById('horarioacordar')

form.addEventListener("submit", (e) => {
    e.preventDefault();
})

if (horasDeSono >= 8) {
    document.write(`teste`)
}